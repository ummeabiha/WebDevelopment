import Cylinder from './cylinder.js';

const c1=new Cylinder(3.5,5,9,13);
console.log('The parameters of Cylinder are '+c1.show2());
console.log('The area of circle is '+c1.area());
console.log('The volume of circle is '+c1.volume());
